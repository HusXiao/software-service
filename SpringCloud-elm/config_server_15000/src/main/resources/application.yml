server:
  port: 15000

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
         uri: https://gitee.com/hu-frey/elmconfig.git #git仓库地址
         username: 17587037579
         password: 020223hy

# rabbitmq的配置
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

# 使用bus总线刷新配置文件
  management:
    endpoints:
       web:
          exposure:
             include: bus-refresh #暴露bus-refresh节点，通过此节点刷新配置

#eureka配置
eureka:
    client:
      service-url:
          #将自己注册给defaultZone给定的eureka集群上
          defaultZone: http://eurekaServer13000:13000/eureka/,http://eurekaServer13001:13001/eureka/
      instance:
          prefer-ip-address: true #使用ip地址向eureka进行注册
          instance-id: ${spring.cloud.client.ip-address}:${server.port}
          lease-renewal-interval-in-seconds: 5
          lease-expiration-duration-in-seconds: 15
